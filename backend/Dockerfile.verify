FROM python:3.11-slim
ENV PYTHONUNBUFFERED=1
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY verify.py .
# One-shot acceptance service: runs, reports via exit code, exits.
ENTRYPOINT ["python", "verify.py"]
